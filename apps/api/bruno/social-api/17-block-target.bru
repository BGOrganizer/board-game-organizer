meta {
  name: Actor blocks target
  type: http
  seq: 17
}

put {
  url: {{baseUrl}}/api/blocks/{{targetUserId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{actorToken}}
}

tests {
  test("returns 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
