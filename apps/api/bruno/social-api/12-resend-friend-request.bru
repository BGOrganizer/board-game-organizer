meta {
  name: Actor sends another friend request
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/api/friend-requests/{{targetUserId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{actorToken}}
}

tests {
  test("returns 201", function () {
    expect(res.getStatus()).to.equal(201);
  });
}
