meta {
  name: Actor unblocks target
  type: http
  seq: 20
}

delete {
  url: {{baseUrl}}/api/blocks/{{targetUserId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{actorToken}}
}

tests {
  test("returns 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
