meta {
  name: Actor lists friends
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/api/friends
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{actorToken}}
}

tests {
  test("returns an array", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an("array");
  });
}
