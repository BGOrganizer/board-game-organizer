meta {
  name: Actor cancels outgoing request
  type: http
  seq: 16
}

delete {
  url: {{baseUrl}}/api/friend-requests/{{targetUserId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{actorToken}}
}

tests {
  test("returns 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
