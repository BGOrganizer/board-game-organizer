meta {
  name: Actor removes target friendship
  type: http
  seq: 11
}

delete {
  url: {{baseUrl}}/api/friends/{{targetUserId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{actorToken}}
}

tests {
  test("returns 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
