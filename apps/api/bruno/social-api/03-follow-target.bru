meta {
  name: Actor follows target
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/api/follows/{{targetUserId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{actorToken}}
}

tests {
  test("returns 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
