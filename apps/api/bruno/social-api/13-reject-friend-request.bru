meta {
  name: Target rejects actor request
  type: http
  seq: 13
}

patch {
  url: {{baseUrl}}/api/friend-requests/{{actorUserId}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{targetToken}}
  Content-Type: application/json
}

body:json {
  {
    "decision": "reject"
  }
}

tests {
  test("returns 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
