meta {
  name: Actor lists following
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/follows?direction=following
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{actorToken}}
}

tests {
  test("returns an array", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an("array");
  });
}
