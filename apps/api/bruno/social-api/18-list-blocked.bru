meta {
  name: Actor lists blocked users
  type: http
  seq: 18
}

get {
  url: {{baseUrl}}/api/blocks
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{actorToken}}
}

tests {
  test("returns an array", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an("array");
  });
}
