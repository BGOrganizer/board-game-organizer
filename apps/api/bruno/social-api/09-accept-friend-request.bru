meta {
  name: Target accepts actor request
  type: http
  seq: 9
}

patch {
  url: {{baseUrl}}/api/friend-requests/{{actorUserId}}
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{targetToken}}
  Content-Type: application/json
}

body:json {
  {
    "decision": "accept"
  }
}

tests {
  test("returns 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}
